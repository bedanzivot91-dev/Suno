# GitHub upload status

V1 branch contains the dedicated Suno Pesme Studio V1 area, current Panako/Olaf installer logic, V1 support modules, regression tests, version marker and verified source/package hashes.

The existing main source history is preserved. This directory is intentionally additive so older code is not deleted.
